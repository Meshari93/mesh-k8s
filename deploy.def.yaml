apiVersion: apps/v1
kind: Deployment
metadata:
  name: mesh-app-deploy
  labels: 
   app: mesh-app
   type: front-end
spec:
 replicas: 6
 selector:
  matchLabels: 
   app: mesh-app
   type: front-end
 template:
  metadata: 
   name: first-api-dec
   labels:
    app: mesh-app
    type: front-end 
  spec:
   containers:
   - name: simple-api
     image: meshach93/meshed:v1
     ports:
        - containerPort: 80